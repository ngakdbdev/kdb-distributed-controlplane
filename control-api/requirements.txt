fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlmodel==0.0.22
alembic==1.13.3
psycopg2-binary==2.9.9
pymysql==1.1.1
pyodbc==5.1.0
docker==7.1.0
kubernetes==30.1.0
qpython3==1.0.1
numpy<2
pyarrow==17.0.0
# background query export to cloud storage (app/export_sinks.py) - lazily
# imported only when that destination is actually used, but pinned here so
# the image always has them (small, no native-lib licensing concerns like
# msodbcsql18 has).
boto3==1.35.36
azure-storage-blob==12.23.1
azure-identity==1.19.0
# app/sso.py (Entra OIDC token exchange + JWKS fetch) imports this directly,
# unconditionally, and app/main.py always loads that router - this app will
# not start without it. Previously undeclared here and only working because
# docker==7.1.0/kubernetes==30.1.0 happen to pull it in transitively - fragile
# (a future bump to either dropping requests would break app startup with no
# warning) so pinned explicitly like every other direct import in this file.
requests==2.34.2
pyjwt==2.9.0
bcrypt==4.2.0
cryptography==43.0.1
python-multipart==0.0.9
websockets==13.1
ldap3>=2.9
anthropic>=0.40.0
# real Prometheus exposition-format /metrics endpoint (app/prometheus_metrics.py) -
# renders from the SAME live data the JSON dashboard endpoints already use,
# not a separate collection pipeline.
prometheus-client==0.21.0
